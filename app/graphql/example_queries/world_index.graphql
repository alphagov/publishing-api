fragment worldLocationInfo on WorldLocation {
  active
  name
  slug
}

query worldIndex {
  worldIndex {
    title

    worldLocations {
      ...worldLocationInfo
    }

    internationalDelegations {
      ...worldLocationInfo
    }
  }
}
