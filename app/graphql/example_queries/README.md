# GraphQL queries

These files aren't read by the GraphQL API or the wider Ruby/Rails app. They're
here for documentation purposes, and may be removed if/when we shift to frontend
applications querying the API
